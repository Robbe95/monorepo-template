import { addressCollection } from '@payload/collections/users/address/address.collection'
import { userCollection } from '@payload/collections/users/user/user.collection'

export const userCollections = [
  userCollection,
  addressCollection,
]
